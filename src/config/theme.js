const lightTheme = {
  body: '#fff',
  text: '#606060',
  toggleFill: '#F21905',
  toggleBtn: '#fff'
};

const darkTheme = {
  body: '#000020',
  text: '#fff',
  toggleFill: '#fff',
  toggleBtn: '#F21905'
};

const LIGHT = 'light';
const DARK = 'dark';

export {
  lightTheme,
  darkTheme,
  LIGHT,
  DARK
};
